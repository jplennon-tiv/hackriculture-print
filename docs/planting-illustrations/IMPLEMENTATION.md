# Pilot production widget — resume here

16 September 2026. In progress. User chose the existing-column design and authorised production implementation for beetroot, carrot, potato, leek and chicory. No full-width rollout. Normal single/batch export must remain AI-free.

Pre-change snapshot: `/Users/johnlennon/Documents/web_site/hackriculture-data/backups/documentation/2026-09-16-planting-production-start.kz56WQ/` (src, plugin files, docs and unchanged master).

Plan: preserve all existing master leaves; add a separate `print_planting` editorial companion with granular steps, source references and a review fingerprint. Presentation-only image/layout/measurement bindings live in print code. Freeze neighbouring content after the old text-only baseline fit, then fit the illustrated card independently using approved optional notes and bounded image sizes. Source changes flag review; missing artwork falls back to the same instructions without pictures. Validate schema/admin round trips, all pilots in both units and A4/A5/A6, actual PDF text/page counts and visual renders. Maintain checkpoints before data/code updates.

Current action: capture fresh baseline PDFs; implement types/schema, source-aware content helpers, artwork mapping, React card and two-phase fitter. No data edits or runtime widget installed yet.
